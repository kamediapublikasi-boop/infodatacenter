{
  "name": "infodatacenter",
  "version": "1.0.0",
  "private": true,
  "description": "Pusat Informasi Kegiatan Gereja - web dashboard multi-admin (Vercel + Supabase)",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node seed.js",
    "check": "node --check server/index.js && node --check server/db.js && node --check server/pin.js && node --check server/csv.js && node --check seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
